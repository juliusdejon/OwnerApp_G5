import { View } from "react-native";

function Card() {
  return (
    <View
      style={{
        width: "100%",
        height: 200,
        marginTop: 10,
        borderRadius: 20,
        borderWidth: 0.5,
        borderColor: "#cecece",
      }}
    ></View>
  );
}

export default Card;
